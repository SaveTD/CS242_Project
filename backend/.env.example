# Environment variables สำหรับ Restroom Finder Backend
# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่าตามจริง

# Database URL
# SQLite (ใช้ตอน dev/local)
DATABASE_URL=sqlite:///./restroom.db

# PostgreSQL (ใช้ตอน deploy จริง)
# DATABASE_URL=postgresql://user:password@localhost:5432/restroom_db
